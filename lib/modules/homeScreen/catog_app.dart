import 'package:flutter/material.dart';

class Catog extends StatelessWidget {
  const Catog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "catogery"
        
      ),
    );
  }
}